

var render = (function () {


  function renderToString(component) {
    //先执行组件函数得到html模板
    var template = component();
    //判断是否有自定义组件
    if (true) {
      //如果有则递归获取html模板
    }
  }

  function render(component) {

  }

  return render;
})()